% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ana_reference_data.R
\name{get_ana_stations}
\alias{get_ana_stations}
\title{Consultar o inventário de estações da ANA}
\usage{
get_ana_stations(
  station_code = NULL,
  state_code = NULL,
  basin_code = NULL,
  token = NULL,
  ...,
  data_source = "api",
  source = data_source
)
}
\arguments{
\item{station_code}{Código(s) de estação ANA, como texto ou número.}

\item{state_code}{Sigla(s) de unidade federativa, como \code{"MG"} ou \code{"SP"}.}

\item{basin_code}{Código(s) de bacia.}

\item{token}{Token retornado por \code{ana_authenticate()}. Se \code{NULL}, a função
tenta autenticar com credenciais disponíveis nos argumentos ou no ambiente.}

\item{...}{Argumentos adicionais usados internamente, como \code{timeout},
\code{max_attempts}, \code{retry_sleep_seconds} e opções de teste.}

\item{data_source}{Fonte dos dados. Na prática, esta função usa a rota
autenticada de inventário da ANA.}

\item{source}{Alias de compatibilidade para \code{data_source}.}
}
\value{
\code{data.frame} com metadados padronizados das estações retornadas pela
ANA.
}
\description{
Consulta o inventário de estações pelo serviço autenticado da ANA e retorna
metadados padronizados dos postos encontrados. A função pode ser usada para
buscar uma estação específica, estações de uma unidade federativa ou estações
associadas a uma bacia.
}
\examples{
# Exemplo operacional. Requer credenciais validas da ANA.
if (FALSE) {
  token <- ana_authenticate()
  estacoes <- get_ana_stations(state_code = "MG", token = token)
  head(estacoes)
}
}
