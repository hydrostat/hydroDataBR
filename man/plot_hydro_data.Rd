% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hydro-output.R
\name{plot_hydro_data}
\alias{plot_hydro_data}
\title{Gerar gráficos hidrológicos}
\usage{
plot_hydro_data(
  data,
  plot = c("daily_series", "availability", "monthly_summary", "annual_summary",
    "flow_duration", "rating_curves", "rating_validity", "measurements",
    "hydrometry_diagnostics", "cross_section_profile", "cross_section_overlay",
    "cross_section_timeline", "annual_maxima", "low_flows", "rainfall_indices",
    "rainfall_annual_maxima", "rainfall_monthly_boxplot", "rainfall_diagnostics",
    "measurement_diagnostics", "rating_diagnostics"),
  variable = NULL,
  station_code = NULL,
  type = NULL,
  multi_station = NULL,
  max_facets = 12,
  ...
)
}
\arguments{
\item{data}{Objeto de dados. Pode ser uma série diária padronizada, um objeto
retornado por \code{get_ana_data()}, um lote retornado por \code{get_ana_data_batch()}
ou uma tabela/lista de resultados de análise.}

\item{plot}{Tipo de gráfico. Valores aceitos incluem \code{"daily_series"},
\code{"availability"}, \code{"monthly_summary"}, \code{"annual_summary"},
\code{"flow_duration"}, \code{"flow_indices"}, \code{"annual_maxima"},
\code{"low_flows"}, \code{"rainfall_indices"},
\code{"rainfall_annual_maxima"}, \code{"rainfall_monthly_boxplot"},
\code{"rainfall_diagnostics"}, \code{"measurements"},
\code{"measurement_diagnostics"}, \code{"rating_curves"},
\code{"rating_validity"}, \code{"rating_diagnostics"},
\code{"hydrometry_diagnostics"}, \code{"cross_section_profile"},
\code{"cross_section_overlay"} e \code{"cross_section_timeline"}.}

\item{variable}{Variável diária a filtrar, como \code{"discharge"}, \code{"stage"}
ou \code{"rainfall"}.}

\item{station_code}{Código(s) de estação a filtrar.}

\item{type}{Subtipo usado por alguns gráficos, por exemplo em medições de
descarga ou diagnósticos.}

\item{multi_station}{Estratégia para múltiplas estações: \code{"facet"},
\code{"list"}, \code{"overlay"} ou \code{"error"}. Quando \code{NULL}, a função escolhe
um comportamento adequado ao gráfico.}

\item{max_facets}{Número máximo de estações em gráficos facetados.}

\item{...}{Argumentos adicionais do gráfico escolhido, como \code{title},
\code{subtitle}, \code{log_y}, \code{section_id}, \code{section_date} ou \code{base_size}.}
}
\value{
Objeto \code{ggplot}. Em alguns casos de múltiplas estações, pode retornar
uma lista nomeada de objetos \code{ggplot}.
}
\description{
Gera gráficos a partir de séries diárias padronizadas, objetos de aquisição
do hydroDataBR ou resultados de análises. A função reúne os principais
gráficos usados na inspeção de séries, disponibilidade, regime, permanência,
extremos, chuva, medições de descarga, curvas-chave e seções transversais.
Para \code{plot = "hydrometry_diagnostics"}, séries diárias e objetos agregados
podem ser diagnosticados automaticamente com apoio da base hidrométrica
interna quando referências explícitas não forem fornecidas.
}
\examples{
daily <- data.frame(
  station_code = "001",
  date = as.Date("2020-01-01") + 0:29,
  variable = "discharge",
  value = seq(10, 39),
  unit = "m3/s",
  consistency_level = NA_integer_,
  source_status = NA_character_,
  source = "example"
)

plot_hydro_data(daily, plot = "daily_series")
plot_hydro_data(daily, plot = "flow_duration")
}
