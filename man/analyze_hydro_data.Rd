% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze-hydro.R
\name{analyze_hydro_data}
\alias{analyze_hydro_data}
\title{Analisar dados hidrológicos padronizados}
\usage{
analyze_hydro_data(
  data,
  analysis = c("daily_statistics", "daily_availability", "flow_duration", "flow_indices",
    "monthly_flow_indices", "rainfall_indices", "annual_maxima",
    "rainfall_annual_maxima", "low_flows", "rainfall_diagnostics", "daily_gap_summary",
    "hydrometry_diagnostics", "measurement_diagnostics", "rating_diagnostics"),
  variable = NULL,
  station_code = NULL,
  period = "annual",
  year_start_month = 1L,
  durations = c(1L, 3L, 7L, 15L, 30L),
  wet_day_threshold = 1,
  exceedance_probabilities = 1:99,
  complete_years_only = TRUE,
  ...
)
}
\arguments{
\item{data}{Objeto de dados. Pode ser uma série diária padronizada, um objeto
retornado por \code{get_ana_data()}, ou um lote retornado por
\code{get_ana_data_batch()}.}

\item{analysis}{Tipo de análise. Valores aceitos incluem
\code{"daily_statistics"}, \code{"daily_availability"}, \code{"flow_duration"},
\code{"flow_indices"}, \code{"monthly_flow_indices"}, \code{"annual_maxima"},
\code{"low_flows"}, \code{"rainfall_indices"},
\code{"rainfall_annual_maxima"}, \code{"rainfall_diagnostics"},
\code{"daily_gap_summary"}, \code{"hydrometry_diagnostics"},
\code{"measurement_diagnostics"} e \code{"rating_diagnostics"}.}

\item{variable}{Variável diária a filtrar, como \code{"discharge"}, \code{"stage"}
ou \code{"rainfall"}. Quando \code{NULL}, a função usa a variável adequada para a
análise escolhida.}

\item{station_code}{Código(s) de estação a filtrar.}

\item{period}{Período usado em algumas análises agregadas.}

\item{year_start_month}{Mês inicial do ano de análise. Use 1 para ano civil
e 10 para ano hidrológico de outubro a setembro.}

\item{durations}{Durações, em dias, usadas para mínimas móveis anuais.}

\item{wet_day_threshold}{Limiar, em mm, para definir dia chuvoso nos índices
pluviométricos.}

\item{exceedance_probabilities}{Probabilidades de permanência, em porcentagem,
usadas na curva de permanência. O padrão retorna 1 a 99\%.}

\item{complete_years_only}{Se \code{TRUE}, remove anos incompletos em análises de
máximos anuais e mínimas anuais.}

\item{...}{Argumentos adicionais usados por análises específicas.}
}
\value{
Em geral, um \code{data.frame}. Algumas análises técnicas podem retornar
listas com tabelas auxiliares.
}
\description{
Calcula estatísticas, indicadores e diagnósticos básicos a partir de séries
diárias padronizadas ou de objetos retornados por funções de aquisição do
pacote. A função não baixa dados e não acessa serviços externos; ela trabalha
apenas com objetos já carregados na sessão.
}
\details{
A função foi pensada para o fluxo típico de engenharia hidrológica: obter ou
ler os dados, padronizar a série diária e então calcular indicadores de
disponibilidade, regime, permanência, extremos e consistência. Para vazões,
as análises incluem curva de permanência, QMLT, Q90, Q95, índices mensais,
máximos anuais e mínimas móveis. Para chuva, incluem índices anuais, máximos
anuais e diagnósticos de falhas/consistência. Diagnósticos fluviométricos
podem usar a base hidrométrica interna do pacote quando as referências não
são fornecidas explicitamente.
}
\examples{
daily <- data.frame(
  station_code = "001",
  date = as.Date("2020-01-01") + 0:29,
  variable = "discharge",
  value = seq(10, 39),
  unit = "m3/s",
  consistency_level = NA_integer_,
  source_status = NA_character_,
  source = "example"
)

analyze_hydro_data(daily, analysis = "flow_indices")
analyze_hydro_data(daily, analysis = "daily_availability")

# A mesma interface tambem pode ser usada com objetos de aquisicao.
objeto <- list(daily_data = daily)
analyze_hydro_data(objeto, analysis = "flow_duration")
}
