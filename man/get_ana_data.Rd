% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ana-api-windowing.R
\name{get_ana_data}
\alias{get_ana_data}
\title{Obter dados hidrológicos da ANA}
\usage{
get_ana_data(
  product,
  data_source = "webservice",
  station_code = NULL,
  path = NULL,
  ...
)
}
\arguments{
\item{product}{Produto solicitado. Valores usuais incluem \code{"daily_discharge"},
\code{"daily_stage"}, \code{"daily_rainfall"}, \code{"discharge_measurements"},
\code{"rating_curves"}, \code{"cross_sections"}, \code{"stations"},
\code{"states"}, \code{"municipalities"}, \code{"basins"}, \code{"subbasins"},
\code{"rivers"}, \code{"entities"} e \code{"all"}. Para arquivos locais, o produto
diário pode ser inferido a partir do leitor usado.}

\item{data_source}{Origem dos dados. Use \code{"api"} para a API autenticada,
\code{"webservice"} para o WebService online legado, \code{"xml"} para arquivo
XML local da operação \code{HidroSerieHistorica}, ou \code{"hidroweb"} para arquivo
CSV/ZIP local do HidroWeb.}

\item{station_code}{Código da estação ANA. Pode ser texto ou número; códigos
com zeros à esquerda devem ser informados como texto.}

\item{path}{Caminho de arquivo local, quando \code{data_source} for \code{"xml"},
\code{"hidroweb"} ou outra fonte local suportada.}

\item{...}{Argumentos adicionais usados pela rota escolhida. Os mais comuns
são \code{start_date}, \code{end_date}, \code{token}, \code{source}, \code{include_cross_sections},
\code{variables}, \code{timeout}, \code{max_attempts} e opções de repetição de requisições.}
}
\value{
O tipo de objeto depende do produto. Séries diárias retornam um
\code{data.frame} padronizado. Produtos especializados retornam tabelas ou listas
com tabelas. Aquisições agregadas retornam uma lista com os produtos obtidos,
a tabela diária combinada em \code{daily_data} e um \code{request_report} com o estado
de cada requisição.
}
\description{
Obtém dados da ANA por uma interface única. A função cobre leituras locais,
consultas ao WebService legado e consultas à API autenticada, sempre que a
rota correspondente estiver disponível para o produto solicitado.
}
\details{
Esta é a principal porta de entrada para aquisição de dados no hydroDataBR.
Para uso cotidiano, escolha o produto em \code{product}, a origem em \code{data_source}
ou \code{source}, e informe os demais argumentos necessários, como código da
estação, período ou caminho de arquivo local.

Para séries diárias, o retorno segue o contrato padronizado do pacote, com as
colunas \code{station_code}, \code{date}, \code{variable}, \code{value}, \code{unit},
\code{consistency_level}, \code{source_status} e \code{source}. Essa padronização permite
usar diretamente os resultados em \code{analyze_hydro_data()}, \code{plot_hydro_data()},
\code{table_hydro_data()} e \code{write_hydro_data()}.

Com \code{product = "all"} e \code{data_source = "api"}, a função monta uma aquisição
agregada para uma estação. Para postos fluviométricos, são avaliados dados de
vazão diária, cota diária, medições de descarga e curvas-chave; chuva diária
só é solicitada quando houver disponibilidade pluviométrica no inventário. As
seções transversais são opcionais e ficam desativadas por padrão com
\code{include_cross_sections = FALSE}, pois essa rota pode ser lenta ou instável.

As rotas da API autenticada são planejadas em janelas de no máximo um ano. Em
períodos longos, o download pode demorar, retornar resultados vazios para
algumas janelas ou falhar por instabilidade temporária do serviço. Isso é
especialmente comum em produtos especializados, como curvas-chave e seções
transversais. Resultados vazios de \code{rating_curves} podem ser válidos e não
indicam, por si só, erro do pacote.
}
\examples{
# Exemplo operacional com API autenticada. Requer credenciais válidas da ANA.
if (FALSE) {
  token <- ana_authenticate(
    identifier = Sys.getenv("ANA_HIDROWEBSERVICE_IDENTIFIER"),
    password = Sys.getenv("ANA_HIDROWEBSERVICE_PASSWORD")
  )

  dados <- get_ana_data(
    product = "daily_discharge",
    data_source = "api",
    station_code = "56460000",
    start_date = "2020-01-01",
    end_date = "2020-01-31",
    token = token
  )

  agregado <- get_ana_data(
    product = "all",
    data_source = "api",
    station_code = "56460000",
    start_date = "2020-01-01",
    end_date = "2020-12-31",
    include_cross_sections = FALSE,
    token = token
  )
}

# Exemplo local com arquivo ja baixado do HidroWeb.
if (FALSE) {
  dados <- get_ana_data(
    product = "daily_discharge",
    data_source = "hidroweb",
    path = "Estacao_56460000.zip"
  )
}
}
