% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{diagnose_daily_hydrometry}
\alias{diagnose_daily_hydrometry}
\title{Diagnosticar consistência diária entre vazão, cota e curva-chave}
\usage{
diagnose_daily_hydrometry(
  daily_data,
  rating_curves = data.frame(),
  relative_error_threshold_pct = 20,
  measurements = data.frame(),
  rating_curve_summary = data.frame(),
  station_code = NULL,
  use_internal_database = TRUE
)
}
\arguments{
\item{daily_data}{Série diária no contrato padronizado do pacote. Deve conter
pelo menos \code{station_code}, \code{date}, \code{variable} e \code{value}. Linhas com
\code{variable = "discharge"} são tratadas como vazão em m3/s; linhas com
\code{variable = "stage"} são tratadas como cota em cm.}

\item{rating_curves}{Tabela de curvas-chave com datas de validade, limites
de cota e coeficientes da equação. A coluna \code{coefficient_h0_m} também é
aceita por compatibilidade.}

\item{relative_error_threshold_pct}{Limiar absoluto, em porcentagem, para
sinalizar diferença elevada entre vazão observada e vazão calculada pela
curva-chave.}

\item{measurements}{Tabela opcional de medições de descarga. Quando ausente,
a função tenta usar medições presentes em objetos agregados e, por fim, a
base hidrométrica interna do pacote.}

\item{rating_curve_summary}{Tabela opcional de resumo das curvas-chave.}

\item{station_code}{Código(s) de estação a filtrar.}

\item{use_internal_database}{Se \code{TRUE}, usa a base hidrométrica interna do
pacote quando não houver referência fornecida pelo usuário nem no objeto
de aquisição.}
}
\value{
Lista com as tabelas \code{summary}, \code{indices}, \code{daily_flags},
\code{rating_matches}, \code{rating_curve_coverage}, \code{measurement_year_summary},
\code{hydrometry_reference_summary} e metadados da referência usada.
}
\description{
Compara séries diárias de vazão e cota com os segmentos de curva-chave
disponíveis. A função identifica lacunas cruzadas entre vazão e cota, valores
não positivos, períodos sem curva aplicável, cotas fora da faixa de validade
da curva e diferenças entre a vazão observada e a vazão estimada pela
curva-chave.
}
\details{
Quando medições de descarga ou curvas-chave não são fornecidas pelo usuário
nem estão presentes em um objeto agregado de \code{get_ana_data(product = "all")},
a função consulta a base hidrométrica interna do pacote, correspondente a um
retrato da ANA de junho de 2026. O objeto retornado informa a origem da
referência hidrométrica usada.
}
\examples{
daily_data <- data.frame(
  station_code = "001",
  date = as.Date(c("2020-01-01", "2020-01-01")),
  variable = c("discharge", "stage"),
  value = c(10, 100)
)

rating_curves <- data.frame(
  station_code = "001",
  valid_from = as.Date("2019-01-01"),
  valid_to = as.Date("2021-12-31"),
  stage_min_cm = 50,
  stage_max_cm = 200,
  coefficient_a = 10,
  coefficient_h0_cm = 0,
  coefficient_n = 1
)

diagnostico <- diagnose_daily_hydrometry(daily_data, rating_curves)
diagnostico$summary
}
