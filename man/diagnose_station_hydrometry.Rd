% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{diagnose_station_hydrometry}
\alias{diagnose_station_hydrometry}
\title{Diagnosticar medições de descarga e curvas-chave}
\usage{
diagnose_station_hydrometry(
  measurements = data.frame(),
  rating_curves = data.frame(),
  params = NULL,
  detailed = TRUE,
  rating_curve_summary = data.frame(),
  station_code = NULL,
  use_internal_database = TRUE
)
}
\arguments{
\item{measurements}{Tabela de medições de descarga, ou objeto agregado
retornado por \code{get_ana_data(product = "all")} contendo o elemento
\code{discharge_measurements}.}

\item{rating_curves}{Tabela de curvas-chave. Se \code{measurements} for um objeto
agregado e este argumento estiver vazio, a função tenta usar o elemento
\code{rating_curves} do próprio objeto.}

\item{params}{Lista opcional de parâmetros diagnósticos. Valores omitidos
usam os padrões internos.}

\item{detailed}{Se \code{TRUE}, calcula pareamento, resíduos, envelopes e triagem
temporal. Se \code{FALSE}, calcula apenas métricas leves.}

\item{rating_curve_summary}{Tabela opcional de resumo das curvas-chave.}

\item{station_code}{Código(s) de estação a filtrar. Também permite consultar
a base interna quando \code{measurements} estiver vazio.}

\item{use_internal_database}{Se \code{TRUE}, usa a base hidrométrica interna do
pacote quando não houver referência fornecida pelo usuário nem no objeto
de aquisição.}
}
\value{
Lista com tabelas de resumo, índices, flags de medições, metadados
das curvas, pareamentos e resultados de resíduos quando \code{detailed = TRUE}.
}
\description{
Avalia a consistência geral entre medições de descarga e curvas-chave de uma
estação. A função calcula indicadores de qualidade, verifica valores nulos ou
negativos, identifica grupos repetidos, pareia medições com segmentos de
curva-chave e, quando solicitado, calcula resíduos e envelopes empíricos.
}
\details{
Quando medições de descarga ou curvas-chave não são fornecidas pelo usuário
nem estão presentes em um objeto agregado de \code{get_ana_data(product = "all")},
a função consulta a base hidrométrica interna do pacote, correspondente a um
retrato da ANA de junho de 2026. O objeto retornado informa a origem da
referência hidrométrica usada.

Os resultados são exploratórios e servem para apoiar revisão hidrométrica.
Eles não substituem a avaliação técnica de consistência, nem a revisão
especializada de uma curva-chave.
}
\examples{
measurements <- data.frame(
  station_code = "001",
  measurement_date = as.Date(c("2020-01-01", "2020-02-01")),
  stage_cm = c(100, 120),
  discharge_m3s = c(10, 15)
)

curves <- data.frame(
  station_code = "001",
  rating_curve_id = "rc1",
  rating_curve_segment_id = "seg1",
  segment_number = 1,
  valid_from = as.Date("2019-01-01"),
  valid_to = as.Date("2021-12-31"),
  stage_min_cm = 50,
  stage_max_cm = 200,
  coefficient_a = 10,
  coefficient_h0_cm = 0,
  coefficient_n = 1
)

diagnose_station_hydrometry(measurements, curves, detailed = FALSE)$summary
}
