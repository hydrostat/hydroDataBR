% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ana-main.R
\name{get_ana_data_batch}
\alias{get_ana_data_batch}
\title{Obter dados da ANA para várias estações}
\usage{
get_ana_data_batch(
  product,
  station_code = NULL,
  ...,
  station_codes = NULL,
  data_source = "webservice",
  path = NULL
)
}
\arguments{
\item{product}{Produto desejado. Use os mesmos valores aceitos por
\code{get_ana_data()}, incluindo \code{"all"} para aquisição agregada por estação.}

\item{station_code}{Vetor de códigos de estação.}

\item{...}{Argumentos adicionais repassados a \code{get_ana_data()} ou à rota
agregada, como \code{start_date}, \code{end_date}, \code{token}, \code{source},
\code{include_cross_sections}, \code{timeout} e opções de repetição.}

\item{station_codes}{Alias de compatibilidade para \code{station_code}.}

\item{data_source}{Origem dos dados. Ver \code{get_ana_data()}.}

\item{path}{Caminho de arquivo local, quando aplicável.}
}
\value{
Lista com \code{results}, contendo os resultados por estação, e
\code{request_report}, contendo o relatório consolidado das requisições.
}
\description{
Executa \code{get_ana_data()} para um conjunto de estações e organiza os resultados
em uma lista padronizada. A função é útil quando o mesmo produto deve ser
obtido para vários postos, mantendo um relatório único de sucesso, falha,
resultado vazio ou produto ignorado.
}
\details{
Em aquisições agregadas com \code{product = "all"}, códigos repetidos são tratados
uma única vez. O relatório informa a ordem do lote e permite separar casos de
sucesso, erro, resposta vazia e produtos pulados por regra de disponibilidade.

Para downloads longos via API, especialmente com produtos especializados, é
recomendável começar com poucos postos e janelas curtas de tempo, avaliar o
\code{request_report} e só depois ampliar o lote.
}
\examples{
# Exemplo operacional. Requer credenciais validas da ANA.
if (FALSE) {
  token <- ana_authenticate()
  lote <- get_ana_data_batch(
    product = "all",
    station_code = c("56460000", "40100000"),
    data_source = "api",
    start_date = "2020-01-01",
    end_date = "2020-12-31",
    include_cross_sections = FALSE,
    token = token
  )
  table_hydro_data(lote, table = "request_report")
}
}
