% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write-hydro-data.R
\name{write_hydro_data}
\alias{write_hydro_data}
\title{Exportar dados, tabelas e gráficos hidrológicos}
\usage{
write_hydro_data(
  x,
  path,
  format = c("csv", "rds", "png", "pdf"),
  components = "all",
  overwrite = FALSE,
  manifest = FALSE,
  hydrological_year_start = 10L,
  low_flow_durations = c(3L, 7L, 15L, 30L),
  ...
)
}
\arguments{
\item{x}{Objeto a exportar. Pode ser série diária padronizada, objeto de
aquisição, lote, tabela, lista de tabelas ou gráfico.}

\item{path}{Caminho de saída. Para múltiplos componentes, informe um
diretório. Para um único arquivo, informe o caminho completo com extensão.}

\item{format}{Formato de exportação: \code{"csv"}, \code{"rds"}, \code{"png"} ou
\code{"pdf"}. Se omitido, a extensão de \code{path} é usada quando reconhecida;
caso contrário, o padrão é \code{"csv"}.}

\item{components}{Componentes a exportar. Use \code{"all"} para gravar todos os
componentes disponíveis, ou informe nomes/aliases como \code{Tab01}, \code{Tab02} e
outros componentes definidos pelo pacote.}

\item{overwrite}{Se \code{TRUE}, sobrescreve arquivos existentes.}

\item{manifest}{Se \code{TRUE} e \code{path} for um diretório, grava também um arquivo
\code{hydro_export_manifest.csv} com os arquivos produzidos.}

\item{hydrological_year_start}{Mês inicial do ano hidrológico usado em
tabelas de máximos anuais. O padrão é 10, representando outubro.}

\item{low_flow_durations}{Durações, em dias, usadas em tabelas de mínimas
móveis anuais.}

\item{...}{Argumentos adicionais. Para gráficos, são repassados a
\code{ggplot2::ggsave()} quando aplicável.}
}
\value{
Invisivelmente, um \code{data.frame} com os arquivos gravados.
}
\description{
Exporta produtos gerados pelo hydroDataBR para arquivos locais. A função pode
gravar séries padronizadas, tabelas finais, relatórios de requisição,
resultados de análise e gráficos \code{ggplot}, dependendo do objeto informado e
do formato escolhido.
}
\details{
A função procura componentes exportáveis dentro do objeto informado. Em séries
diárias, aplica a regra usual do pacote: dado consistido tem prioridade; na
ausência dele, é usado o dado não consistido disponível. Para objetos de
aquisição agregada, a função pode exportar séries, tabelas derivadas e
relatórios. Para gráficos, use formatos como \code{png} ou \code{pdf}.
}
\examples{
daily <- data.frame(
  station_code = "001",
  date = as.Date("2020-01-01") + 0:29,
  variable = "discharge",
  value = seq(10, 39),
  unit = "m3/s",
  consistency_level = NA_integer_,
  source_status = NA_character_,
  source = "example"
)

# Exemplo de exportacao local.
if (FALSE) {
  write_hydro_data(
    daily,
    path = "saida_hydrodatabr",
    components = "all",
    overwrite = TRUE,
    manifest = TRUE
  )
}
}
