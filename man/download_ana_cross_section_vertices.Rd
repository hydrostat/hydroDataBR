% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download-optional-data.R
\name{download_ana_cross_section_vertices}
\alias{download_ana_cross_section_vertices}
\title{Baixar vértices opcionais de seções transversais}
\usage{
download_ana_cross_section_vertices(
  path = tools::R_user_dir("hydroDataBR", "cache"),
  version = "2026-06",
  read = TRUE,
  overwrite = FALSE,
  quiet = FALSE,
  url = NULL,
  metadata_url = NULL
)
}
\arguments{
\item{path}{Caminho de diretório onde o arquivo será salvo. Por padrão, usa o
diretório de cache do usuário para o pacote.}

\item{version}{Versão do retrato de dados. Atualmente apenas \code{"2026-06"} é
suportado.}

\item{read}{Valor lógico. Se \code{TRUE}, lê e retorna o objeto R após baixar ou
localizar o arquivo em cache. Se \code{FALSE}, retorna apenas o caminho local do
arquivo.}

\item{overwrite}{Valor lógico. Se \code{TRUE}, força novo download mesmo quando o
arquivo já existe no cache.}

\item{quiet}{Valor lógico. Se \code{TRUE}, reduz mensagens durante o download.}

\item{url}{URL alternativa ou caminho local alternativo para o arquivo \code{.rds}.
Este argumento é principalmente útil para testes.}

\item{metadata_url}{URL alternativa ou caminho local alternativo para o
arquivo de metadados \code{.csv}. Este argumento é principalmente útil para
testes.}
}
\value{
Se \code{read = TRUE}, um \code{data.frame} com os vértices de seções
transversais. Se \code{read = FALSE}, o caminho local do arquivo baixado.
}
\description{
Baixa o arquivo opcional com os vértices completos de seções transversais da
ANA incluído no repositório GitHub do pacote, mas não instalado junto com o
pacote principal.
}
\details{
O pacote principal inclui metadados e resumos de seções transversais, mas não
inclui os vértices completos dos perfis. Esta função permite baixar esses
vértices quando o usuário precisar reconstruir ou analisar os perfis
transversais completos.

O download usa cache local do usuário. A função não consulta serviços vivos
da ANA e não exige credenciais.
}
\examples{
if (FALSE) {
  vertices <- download_ana_cross_section_vertices()
  head(vertices)

  path <- download_ana_cross_section_vertices(read = FALSE)
  path
}

}
